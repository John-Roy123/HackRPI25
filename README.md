##To run

```npm install vite```
```npm install @google/genai```

Create a .env file in the root directory of the project with the variable

```VITE_GEMINI_API_KEY={your API key}``` 
Then run it with 
```npm run```
